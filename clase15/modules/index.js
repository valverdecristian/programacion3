// estoy importando el archivo series.js
const series = require("./series.js")

// modulo nativo de node
const fs = require ("fs");

const moment = require("moment");

// modulo de terceros (npm)
// con "npm install PACKAGE" traemos las dependencias que se encuentran en el package.json

// el archivo package.json es escencial para trabajar con node

// para iniciar un servidor con node se debe hacer
// npm init -y

console.log(moment);
